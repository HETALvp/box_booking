/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import App from './App';

import {View, Text} from 'react-native';
import React from 'react';

AppRegistry.registerComponent(appName, () => AppAgain);
